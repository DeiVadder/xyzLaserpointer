#include <iostream>

#include "positionmapper.h"

int main()
{
//    XyControl io;
//    std::thread ioThread(&XyControl::run, &io);

//    while(true)
//    {
//        std::this_thread::sleep_for(std::chrono::seconds(1));
////        io.setTargetX(io.targetX() + 100);
////        io.setTargetY(io.targetY() - 100);
//        std::cout << "x and y increased " << std::endl;
//    }

    return 0;
}
